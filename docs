# 1. Install Git LFS
git lfs install

# 2. Tell Git LFS to handle all .pth files
git lfs track "*.pth"

# 3. Add the .gitattributes file that Git LFS created
git add .gitattributes

# 4. Add all your other project files
git add .

# 5. Commit your changes
git commit -m "Add model and project files"

# 6. Push everything to GitHub
git push origin main
